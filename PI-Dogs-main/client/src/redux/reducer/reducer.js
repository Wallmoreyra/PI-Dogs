// importación de las action-types

// initial state:
let initialState = {
    allDogs:[],
    dogsCopy:[],
    posts:[],
    allState:[]
}

// definir la function rootReducer:

function rootReducer(state = initialState, action){

    switch(action.type) {
        case value:

            break;

        default:
            break;
    }
}

export default rootReducer;